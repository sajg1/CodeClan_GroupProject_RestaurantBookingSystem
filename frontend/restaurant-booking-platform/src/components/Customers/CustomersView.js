import React from 'react';
import CustomersList from './CustomersList';

const CustomersView = (props) => {
  return(
    <div>
      <CustomersList bookings={this.props.bookings}/>
    </div>
  )
}

export default CustomersView;
